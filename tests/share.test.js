const{test}=require('node:test');const assert=require('node:assert/strict');const{links,send}=require('../public/share');
test('website and room destinations are distinct; private room passwords round-trip safely',()=>{const x=links('https://playmakruk.com','abc123',true,'ไทย&#+? space');assert.equal(x.website,'https://playmakruk.com/');const u=new URL(x.room);assert.equal(u.pathname,'/room.html');assert.equal(u.searchParams.get('id'),'abc123');assert.equal(u.searchParams.get('pw'),'ไทย&#+? space');assert.equal(new URL(links('https://playmakruk.com','abc123',false,'secret').room).searchParams.has('pw'),false);});
test('cancelling native share never copies a link without asking',async()=>{let copied=false;const n={share:async()=>{throw Object.assign(Error(),{name:'AbortError'})},clipboard:{writeText:async()=>{copied=true}}};assert.equal(await send(n,{url:'https://playmakruk.com/'}),'cancelled');assert.equal(copied,false);});
test('share fallback copies exactly the selected destination',async()=>{let text;assert.equal(await send({clipboard:{writeText:async s=>{text=s}}},{url:'https://playmakruk.com/room.html?id=abc123'}),'copied');assert.equal(text,'https://playmakruk.com/room.html?id=abc123');});
